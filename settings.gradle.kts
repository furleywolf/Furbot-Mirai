pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
        maven("https://dl.bintray.com/kotlin/kotlin-eap")
    }
}
rootProject.name = "furbot-mirai"